rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    match /users/{userId} {
      // Allow users to create their own profile document
      allow create: if isOwner(userId);
      // Allow authenticated users to read and update their own profile
      allow read, update: if isOwner(userId);
      // Deny deletion and listing of user profiles from the client
      allow delete, list: if false;
    }
    
    match /projects/{projectId} {
      // Allow authenticated users to create projects
      allow create: if request.auth != null;
      // Allow users to read, update, and delete their own projects
      allow read, update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
       // Allow authenticated users to list their own projects
      allow list: if request.auth != null;
    }
    
    match /contact_leads/{contactLeadId} {
      // Anyone can submit the contact form.
      allow create: if true;
      // No one can read/update/delete from the client. This data is for backend use.
      allow read, update, delete: if false;
    }

    match /testimonials/{testimonialId} {
      // Anyone can view testimonials.
      allow get, list: if true;
      // No one can create/update/delete from the client.
      allow create, update, delete: if false;
    }
  }
}
